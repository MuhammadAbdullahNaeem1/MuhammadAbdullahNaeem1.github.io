// The page's sections, in the order they appear. Each string is both the
// section's id (see the AppWrap call at the bottom of each container) and the
// label shown in the navbar — so this list drives the navbar links, the mobile
// menu, and the navigation dots. Add a section here once and all three follow.
const sections = ["home", "about", "work", "skills", "certifications", "contact"];

export default sections;
